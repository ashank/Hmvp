<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashank.animation">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AnimationActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityanimation.OneActivity" />
        <activity android:name=".activityanimation.TwoActivity" />
        <activity android:name=".activityanimation.ThreeActivity" />
        <activity android:name=".activityanimation.FourActivity" />
        <activity android:name=".contenttransitions.ContentShareOneActivity" />
        <activity android:name=".contenttransitions.ContentShareTwoActivity"></activity>
    </application>

</manifest>